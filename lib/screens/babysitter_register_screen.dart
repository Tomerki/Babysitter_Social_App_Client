import 'package:flutter/material.dart';

class BabysitterRegisterScreen extends StatelessWidget {
  static const routeName = 'babysitter-register-screen';
  const BabysitterRegisterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Babysitter'),
    );
  }
}
